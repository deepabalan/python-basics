# enumerate

print enumerate('abcd')

# traverse the elements of a sequence and their indices, use the
# built-in function enumerate:

for index, element in enumerate('abc'):
    print index, element
